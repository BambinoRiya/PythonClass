class Solution:
    def percentageLetter(self, s: str, letter: str) -> int:
        newString = ""

        for letter in s:
            len(s) == newString

        return newString

        sumNum = (newString/len(s))
        percentage = sumNum * 100

        return percentage
